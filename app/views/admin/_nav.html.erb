<div class="navbar navbar-light bg-primary">
  <span class="navbar-brand fw-semibold text-light"><%= t(".admin_panel")%></span>

  <div class="text-light">
    <%= t(".greeting") %>, <b class="nav-admin user-email"><%= current_user.email %></b>
    <%= link_to t(".logout"), destroy_user_session_path, data: {turbo_method: :delete} %>
  </div>
</div>
